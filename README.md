# Basic-Project-with-Langchain-Expression-Language-

LCEL is a domain specific language inside the langchain framework ,it gives ways to build AI application pipelines.
We can connect prompt templates ,language models ,retrievers and output parsers.Using (|).
Uses standard method -.invoke().pipe operator(|)=used for composition of sequence of operations on input.
Here in this code chain created with,prompt,messages and the model to be used using pipe operator.
